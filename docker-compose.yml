version: '3.8'
services:
  db:
    image: postgres:15-alpine
    container_name: fts_db
    environment:
      POSTGRES_USER: fts_user
      POSTGRES_PASSWORD: secure_password
      POSTGRES_DB: fts_prod
    ports:
      - "5432:5432"
